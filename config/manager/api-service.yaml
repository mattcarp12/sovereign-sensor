apiVersion: v1
kind: Service
metadata:
  name: controller-manager-api
  namespace: system
  labels:
    control-plane: controller-manager
spec:
  selector:
    control-plane: controller-manager
  ports:
    - name: api
      protocol: TCP
      port: 8080
      targetPort: 8080