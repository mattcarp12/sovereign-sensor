---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: sovereigntypolicies.sec.sovereign.io
spec:
  group: sec.sovereign.io
  names:
    kind: SovereigntyPolicy
    listKind: SovereigntyPolicyList
    plural: sovereigntypolicies
    shortNames:
    - sp
    singular: sovereigntypolicy
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SovereigntyPolicy is the Schema for the sovereigntypolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SovereigntyPolicySpec defines the desired state of SovereigntyPolicy
            properties:
              actions:
                description: |-
                  Actions defines what to do when a policy matches.
                  Valid values: "log", "block-kill" (SIGKILL process), "block-noconn" (close connection)
                items:
                  enum:
                  - allow
                  - log
                  - block
                  type: string
                type: array
              allowedCountries:
                description: AllowedCountries defines the ISO-3166-1 alpha-2 country
                  codes that are permitted
                items:
                  type: string
                type: array
              description:
                description: Description provides human-readable context for the policy
                type: string
              disallowedCountries:
                description: DisallowedCountries defines the ISO-3166-1 alpha-2 country
                  codes that are explicitly blocked
                items:
                  type: string
                type: array
              namespaces:
                description: Namespaces defines which Kubernetes namespaces this policy
                  applies to
                items:
                  type: string
                type: array
            required:
            - actions
            type: object
          status:
            description: SovereigntyPolicyStatus defines the observed state of SovereigntyPolicy
            properties:
              discoveredViolatorIPs:
                description: |-
                  DiscoveredViolatorIPs is populated by the Data Plane Agent.
                  The Reconciler uses this list to dynamically generate Tetragon TracingPolicies.
                items:
                  type: string
                type: array
              state:
                description: State represents the current processing state of the
                  policy
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
