apiVersion: cilium.io/v1alpha1
kind: TracingPolicy
metadata:
  name: sovereign-tcp-monitor
spec:
  kprobes:
  - call: "tcp_connect"
    syscall: false
    args:
    - index: 0
      type: "sock"
    selectors:
    - matchPIDs:
      - operator: NotIn
        values:
        - 1