---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: sovereigntypolicies.sec.sovereign.io
spec:
  group: sec.sovereign.io
  names:
    kind: SovereigntyPolicy
    listKind: SovereigntyPolicyList
    plural: sovereigntypolicies
    shortNames:
    - sp
    singular: sovereigntypolicy
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SovereigntyPolicy is the Schema for the sovereigntypolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PolicySpec defines the desired state of the SovereigntyPolicy.
            properties:
              action:
                description: Action defines the enforcement behavior when a violation
                  occurs.
                enum:
                - allow
                - log
                - block
                type: string
              allowedCountries:
                description: AllowedCountries is a list of ISO 3166-1 alpha-2 country
                  codes.
                items:
                  type: string
                type: array
              description:
                description: Description provides human-readable context for the rule.
                type: string
              namespaces:
                description: Namespaces to which this policy applies. Use "*" for
                  a global default.
                items:
                  type: string
                minItems: 1
                type: array
            required:
            - action
            - namespaces
            type: object
        type: object
    served: true
    storage: true
